{
  "name": "travelease",
  "version": "1.0.0",
  "description": "Travel website",
  "source": "index.html",
  "scripts": {
    "start": "parcel serve index.html",
    "build": "parcel build index.html --no-cache --dist-dir build --public-url ./",
    "lint:html": "htmlhint '**/*.html'",
    "lint:css": "stylelint '**/*.css'"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "htmlhint": "^1.1.5",
    "stylelint": "^16.4.2",
    "stylelint-config-standard": "^36.0.0"
  }
}
